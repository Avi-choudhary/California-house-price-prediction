# California-house-price-prediction
FastAPI practice project — serving a scikit-learn regression model via REST (single &amp; batch CSV prediction, Pydantic validation). Built to learn backend API design while transitioning into ML.
